apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-webServer.fullname" . }}
  labels:
    {{- include "helm-webServer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-webServer.selectorLabels" . | nindent 4 }}
